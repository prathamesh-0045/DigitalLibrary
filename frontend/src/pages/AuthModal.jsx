import React from 'react'
import { Icon, Stat, BookCard, LoanRow, PanelHead, EmptyPanel, Metric } from '../components/common'

function AuthModal({ mode, setMode, form, setForm, onClose, onSubmit, loading, demoAdmin }) { return <div className="modal-backdrop" onMouseDown={e => e.target === e.currentTarget && onClose()}><div className="modal auth-modal"><button className="close" onClick={onClose}>×</button><span className="modal-mark">L</span><span className="eyebrow">WELCOME TO LIBRIS</span><h2>{mode === 'login' ? 'Welcome back.' : 'Create your account.'}</h2><p>{mode === 'login' ? 'Sign in to borrow books and manage your shelf.' : 'Join the library and start building your shelf.'}</p><form onSubmit={onSubmit}>{mode === 'register' && <label>Full name<input required value={form.name} onChange={e => setForm({...form,name:e.target.value})} placeholder="Prathamesh Narale" /></label>}<label>Email<input required type="email" value={form.email} onChange={e => setForm({...form,email:e.target.value})} placeholder="you@example.com" /></label><label>Password<input required minLength="8" type="password" value={form.password} onChange={e => setForm({...form,password:e.target.value})} placeholder="Minimum 8 characters" /></label><button className="primary full" disabled={loading}>{loading ? 'Please wait…' : mode === 'login' ? 'Sign in →' : 'Create account →'}</button></form>{mode === 'login' && <button className="demo-admin" onClick={demoAdmin}>Demo admin: use admin@libris.com / Admin@123</button>}<div className="switch-auth">{mode === 'login' ? <>New here? <button onClick={() => setMode('register')}>Create account</button></> : <>Already a member? <button onClick={() => setMode('login')}>Sign in</button></>}</div></div></div> }

export default AuthModal
